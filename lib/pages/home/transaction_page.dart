import 'package:flutter/material.dart';

class Transctionpage extends StatelessWidget {
  const Transctionpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar:AppBar(title: Text("Trancation Page"),)
    );
  }
}