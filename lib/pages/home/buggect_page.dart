import 'package:flutter/material.dart';

class BuggectPage extends StatelessWidget {
  const BuggectPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Buggect Page")));
  }
}
